<!DOCTYPE html>
<html>
<head>
  <title>QuickGist by Onaxsys | <%= @title; %> </title>
  
	<%= render("layouts/stylesheet"); %>
</head>
	
<body>
<div class="container">
	
	<%=render("layouts/header");%>
	
		<section class="round">
			
			<%= yield %>		
			
		</section>
		
	<%=render("layouts/footer");%>
	<%= debug(params) if Rails.env.development? %>
</div> 
	
</body>
</html>
